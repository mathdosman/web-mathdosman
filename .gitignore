# PHP / Composer
/vendor/
/vendor/*
/composer.phar

# Local config and secrets
/.env
/config/config.php
/.env.local

# Uploads, generated and runtime files
/uploads/
/gambar/
/logs/
/tmp/

# Database snapshots / exports
/*.sql

# IDEs and OS
/.idea/
.vscode/
.DS_Store
Thumbs.db

# Node
/node_modules/

# Build artifacts
/build/
/dist/

# Composer lock usually tracked — keep it unless you prefer otherwise
# vendor directory is excluded because dependencies are installed via Composer
# Runtime / generated
logs/
*.log

# Common PHP/runtime leftovers
error_log
php_errorlog
*.cache
.phpunit.result.cache

# Temp folders
tmp/
temp/
cache/

# Upload directories (keep local)
gambar/

# Student uploads (photos, etc) must stay local
siswa/uploads/

# Local backups
backups/
*.zip

# OS/editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~

# Optional env files
.env
.env.*

# Local app config (contains DB credentials)
config/config.php

# Installer lock file (generated after install)
install/installed.lock

# Adsense ads.txt (keep per-hosting)
ads.txt

# Homepage carousel uploads (keep placeholders in repo; ignore uploaded rasters)
assets/img/carousel/slide*.jpg
assets/img/carousel/slide*.jpeg
assets/img/carousel/slide*.png
assets/img/carousel/slide*.webp
